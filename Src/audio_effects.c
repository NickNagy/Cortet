#include "audio_effects.h"

/* dummy function for ButtonConfigStruct to point to @ initialization (so that it does not crash when trying to pass args to null) */
void dummy_effect(AUDIO_BUFFER_PTR_T buffer, uint16_t size) {
}
